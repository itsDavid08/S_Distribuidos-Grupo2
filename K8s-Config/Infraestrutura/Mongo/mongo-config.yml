# k8s-config/1-infraestrutura/mongo-config.yml
# (Baseado no Lab5.docx)
apiVersion: v1
kind: ConfigMap
metadata:
  name: mongo-config
  namespace: grupo2
data:
  # Service Discovery dinámico - K8s resuelve automáticamente el IP
  # Solo host:puerto (sin mongodb://) ya que el código añade ese prefijo
  mongo-url: mongo-service.grupo2.svc.cluster.local:27017