# k8s-config/1-infraestrutura/mongo-config.yml
# (Baseado no Lab5.docx)
apiVersion: v1
kind: ConfigMap
metadata:
  name: mongo-config
  namespace: grupo2
data:
  # Service Discovery dinámico - K8s resuelve automáticamente el IP
  # Formato: mongodb://nombre-servicio.namespace.svc.cluster.local:port
  mongo-url: mongodb://mongo-service.grupo2.svc.cluster.local:27017